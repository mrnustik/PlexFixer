# Port the web app listens on (default: 3000)
PORT=3000

# Comma-separated absolute paths to your Plex TV Shows libraries
# These must be mounted as volumes in Docker
PLEX_TV_LIBRARY_PATHS=/media/tv

# Comma-separated absolute paths to your Plex Movies libraries
PLEX_MOVIES_LIBRARY_PATHS=/media/movies
